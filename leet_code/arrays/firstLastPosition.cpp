#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    vector<int> searchRange(vector<int>& nums, int target) {
        int high = nums.size() - 1;
        int low = 0;

        while (low < high) {
            int mid = (low + high) / 2;
            if (nums[mid] == target) {

            }
        }
    }
};
int main(){


    return 0;
}