#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    int maximumWealth(vector<vector<int>> &accounts)
    {
        int max_value = INT_MIN;
        for (int i = 0; i < accounts.size(); i++)
        {
            
        }
    }
};