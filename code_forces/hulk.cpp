#include <bits/stdc++.h>
using namespace std;
int main()
{
    int num = 0, i = 1;
    cin >> num;
    while (i <= num)
    {
        if(num == 1) cout <<"I hate it";
        i++;
    }

    return 0;
}