#include <bits/stdc++.h>
using namespace std;
vector<int> reverseArray(vector<int> &arr)
{
    vector<int> ans;
    // reverse(arr.begin(), arr.end());


    return ans;
}
int main()
{
    vector<int> arr = {1, 2, 3, 4, 5};
    reverseArray(arr);
    for (auto x : arr)
        cout << x << endl;
    return 0;
}